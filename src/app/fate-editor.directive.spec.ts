import { FateEditorDirective } from './fate-editor.directive';

describe('FateEditorDirective', () => {
  it('should create an instance', () => {
    const directive = new FateEditorDirective();
    expect(directive).toBeTruthy();
  });
});
